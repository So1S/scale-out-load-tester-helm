apiVersion: v1
kind: Service
metadata:
  name: {{ include "scale-out-load-tester-helm.fullname" . }}
  labels:
    {{- include "scale-out-load-tester-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scale-out-load-tester-helm.selectorLabels" . | nindent 4 }}
